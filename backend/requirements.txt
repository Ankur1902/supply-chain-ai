# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0
python-multipart==0.0.19

# --- Database ---
SQLAlchemy==2.0.36
alembic==1.14.0
PyMySQL==1.1.1
cryptography==44.0.0

# --- Cache / rate limiting ---
redis==5.2.1
slowapi==0.1.9

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pinned <4.1: passlib 1.7.4's version probe reads bcrypt.__about__.__version__,
# which bcrypt removed in 4.1+. Functionally bcrypt still works fine either way
# (this only affects a harmless-but-noisy startup log line), but pinning avoids
# the log noise. See docs/decisions.md.
bcrypt==4.0.1

# --- Data / ML ---
pandas==2.2.3
numpy==1.26.4
pyarrow==18.1.0
scikit-learn==1.5.2
xgboost==2.1.3
shap==0.46.0
joblib==1.4.2
scipy==1.14.1

# --- AI / LLM ---
anthropic==0.40.0
google-generativeai==0.8.3
chromadb==0.5.23
sqlglot==25.34.0

# --- Utilities ---
python-dotenv==1.0.1
httpx==0.28.1
tenacity==9.0.0
structlog==24.4.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
faker==33.1.0
